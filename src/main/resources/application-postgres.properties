database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/system}
spring.datasource.username=${POSTGRES_USER:system}
spring.datasource.password=${POSTGRES_PASS:system}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
